let physicist = Math.random() > 0.5 ? "Marie Curie" : 84;

//Ambos tipos comparten esta propiedad
physicist.toString();

//Error: No todos los tipos tienen esta propiedad
physicist.toUpperCase();
physicist.toFixed();