let rocker;

rocker = "Joan Jett";
rocker.toUpperCase();

rocker = 19.58;
rocker.toPrecision();

rocker.toUpperCase();

let rockandroller: string;

rockandroller = "Joan Jett";

//rockandroller = 19.58; El tipo 'number' no se puede asignar al tipo 'string'.

